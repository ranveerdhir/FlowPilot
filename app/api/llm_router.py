# Endpoint: The endpoint handler will import the function you created in llm_service.py.

# Action: It will then call the service function with the hardcoded prompt and return the result as the HTTP response.

